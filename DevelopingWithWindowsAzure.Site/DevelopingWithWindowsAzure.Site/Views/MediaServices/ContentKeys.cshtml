@model IEnumerable<Microsoft.WindowsAzure.MediaServices.Client.IContentKey>

@{
    ViewBag.Title = "Media Services | Content Keys";
}

<h2>Media Services</h2>

@Html.RenderSubMenu("MediaServices")

<h3>Content Keys</h3>

@Html.RenderGrid(Model, new List<string>() { "EncryptedContentKey" }, 
	deleteAction: (item => Url.Action("DeleteContentKey", new { id=item.Id })))
